name: snap_reels_example
description: Example app for snap_reels

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  snap_reels:
    path: ../

  get: ^4.7.2

flutter:
  uses-material-design: true

dev_dependencies:
  lints: ^5.1.1
